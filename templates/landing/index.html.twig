{% extends 'layouts/unauthenticated.html.twig' %}

{% block content %}
    <h1 class="text-3xl font-bold text-center mb-4">Bienvenue sur Flint</h1>
    <p class="text-center text-gray-600 mb-6">Apprenez efficacement avec la mémorisation espacée</p>

    <div class="flex flex-col gap-4">
        <a href="{{ path('app_login') }}" class="bg-blue-500 text-white p-3 rounded-lg text-center">
            Connexion
        </a>
        <a href="{{ path('app_register') }}" class="bg-green-500 text-white p-3 rounded-lg text-center">
            Inscription
        </a>
    </div>
{% endblock %}
